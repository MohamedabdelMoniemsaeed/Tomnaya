name: tomnaya_app
description: "تمناية - أول تطبيق ذكي لحجز مقاعد وسيارات السوزوكي الفان (الـ 7 راكب) في مصر"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  provider: ^6.1.1
  url_launcher: ^6.2.2
  cloud_firestore: ^4.15.5
  firebase_core: ^2.27.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
